import * as current from 'docs/current';

export const statuses = {
  EXPERIMENTAL: 'experimental',
  BETA: 'beta',
  STABLE: 'stable',
  NEEDS_REVISION: 'revision',
};

export const versions = {
  current,
};
