import React from 'react';
import AlertIcon from './AlertIcon';

export default {
  title: 'Components/AlertIcon',
  component: AlertIcon,
};

export function Default() {
  return <AlertIcon>Default AlertIcon</AlertIcon>;
}
