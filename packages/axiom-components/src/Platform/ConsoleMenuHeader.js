import React, { Component } from "react";
import Base from "../Base/Base";

export default class ConsoleMenuHeader extends Component {
  render() {
    return <Base {...this.props} className="ax-console-menu__header" />;
  }
}
