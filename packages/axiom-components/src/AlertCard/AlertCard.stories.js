import React from 'react';
import AlertCard from './AlertCard';

export default {
  title: 'Components/AlertCard',
  component: AlertCard,
};

export function Default() {
  return <AlertCard>Default AlertCard</AlertCard>;
}
