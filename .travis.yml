language: node_js
cache: yarn
script:
  - yarn lint
  - yarn test
before_install:
  - bash ./ssh.sh
after_success:
  - bash ./deploy.sh
env:
  global:
  - SOURCE_BRANCH: master
  - COMMIT_AUTHOR_NAME: Travis CI
  - COMMIT_AUTHOR_EMAIL: travis@gh-pages
